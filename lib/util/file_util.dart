import 'dart:async';

class FileUtil {
  //TODO 파일 읽어서 문자열 반환
  Future<String> readFileContent({required String filePath}) async {
    await Future.delayed(const Duration(seconds: 1));
    return '//TODO readFileContent ';
  }
}
